import React from 'react';
import { GameObjects } from 'phaser';

type Props = {
  data: GameObjects.Image;
};

function ImageInspector(props: Props) {
  return <div />;
}

export default ImageInspector;
