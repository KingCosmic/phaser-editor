import React from 'react';
import { GameObjects } from 'phaser';

type Props = {
  data: GameObjects.Text;
};

function TextInspector(props: Props) {
  return <div />;
}

export default TextInspector;
